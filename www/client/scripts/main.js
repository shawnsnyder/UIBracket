// Generated by CoffeeScript 1.6.3
require(["app", "router", "backboneredis", "socketio"], function(app, Router, backboneredis, socketio) {
  var socket;
  console.log('main is firing');
  socket = io.connect();
  bbRedis.config({
    io: socket,
    listener: 'backbone'
  });
  app.router = new Router();
  app.initialize();
  Backbone.history.start({
    pushState: false,
    root: app.root
  });
  return $(document).on("click", "a[href]:not([data-bypass])", function(evt) {
    var href, root;
    href = {
      prop: $(this).prop("href"),
      attr: $(this).attr("href")
    };
    root = location.protocol + "//" + location.host + app.root;
    console.log(root);
    if (href.prop.slice(0, root.length) === root) {
      console.log('inside href prop slice');
      evt.preventDefault();
      return Backbone.history.navigate(href.attr, true);
    }
  });
});
